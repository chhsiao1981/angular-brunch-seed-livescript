# Filters

mod = {}
mod.{{project_camelcase}}Filter = <[version]> ++ (version) ->
  (text) ->
    String(text)replace /\%VERSION\%/mg version

[angular.module '{{project}}.filters' <[]> .filter key, val for key, val of mod]

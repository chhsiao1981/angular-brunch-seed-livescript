{{project_camelcase_uppercase}}Ctrl = <[$scope $location $resource $rootScope]> ++ (s, $location, $resource, $rootScope) ->

mod = { {{project_camelcase_uppercase}}Ctrl }

angular.module '{{project}}.controllers' <[{{project}}.services]> .controller mod
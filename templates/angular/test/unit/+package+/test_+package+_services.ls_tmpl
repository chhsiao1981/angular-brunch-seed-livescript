describe "{{project}}.services", (not-it) ->
  beforeEach ->
    module "{{project}}.services"
  
  describe "version", (not-it) ->
    it "should return current version", inject (version) ->
      expect(version).toEqual "0.1"
